<h2><% @artists.each do |artist|%></h2>
<h2><%= artist.name %></h2>
<p><%= artist.songs.count %> song</p>
  <ul>Songs:<% artist.songs.each do |song|%></ul>
  <li><%=link_to song.title, song_path(song) %></li>
  <% end%>
<% end%>
